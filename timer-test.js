/* let countdownDuration = 60;
const countdownNumber = document.getElementsByClassName("inner-number");
let countdown = setInterval(function () {
  if (countdown > 0) {
    countdownNumber.textContent = countdownDuration;
    countdownDuration--;
  } else {
    clearInterval(countdown);
  }
  setInterval();
}, 1000);
 */
